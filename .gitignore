# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
eggs/
.eggs/
# lib/ y lib64/ solo para Python (no afectar frontend/src/lib)
backend/lib/
backend/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Node / pnpm
node_modules/
npm-debug.log
yarn-error.log
pnpm-debug.log
dist/

# Next.js
.next/
out/

# Python / uv
.venv/

# Environment
.env
.env.local
.env.*.local
backend/session.json

# OS
.DS_Store
Thumbs.db

# Tidal — root-level download folder only (not Next.js route dirs)
/downloads/
music/

# SQLite — bases de datos locales de desarrollo
*.db
*.sqlite3
*.db.bak

# Muestras de audio para analisis de espectrograma (Spek) — no versionar
spek_samples/

# Scripts de investigacion/verificacion locales (probes de calidad, PKCE, Android
# Auto). Contienen client_ids/secrets publicos de Tidal hardcodeados → no versionar.
backend/scripts/

# Media
*.mp3
*.flac
*.wav
*.m4a

# Playwright E2E
frontend/playwright-report/
frontend/test-results/
frontend/blob-report/

# Playwright MCP — artefactos de depuracion del navegador (logs, snapshots, capturas)
.playwright-mcp/

# TypeScript — cache incremental de compilacion (se regenera en cada build)
*.tsbuildinfo

# Binario pesado de la version monolitica previa (solo referencia local)
/_old/ffmpeg.exe
